=== MAP NOTES ===
Map UI needs SVG elements so it stretches OK
Player needs listener for map key "m" to toggle visibility of map UI, however map will always be loaded, just not drawn
Map confirmation dialog needs to respond to arrow keys but also suppress 'm' hiding the whole map
Map should autoscale based on project settings but need to confirm

=== INVENTORY NOTES ===
Make the inventory UI and script. There are tutorials. Similar to map UI. Script holds array

=== XXXX NOTES ===
